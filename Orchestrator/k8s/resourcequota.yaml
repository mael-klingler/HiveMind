apiVersion: v1
kind: ResourceQuota
metadata:
  name: hivemind-quota
  namespace: hivemind
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 32Gi
    limits.cpu: "16"
    limits.memory: 64Gi
    pods: "20"
    persistentvolumeclaims: "10"
    configmaps: "100"
    secrets: "20"