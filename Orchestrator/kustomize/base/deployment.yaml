apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestrator
  namespace: hivemind
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orchestrator
  template:
    metadata:
      labels:
        app: orchestrator
    spec:
      serviceAccountName: orchestrator
      imagePullSecrets:
        - name: harbor-registry-secret
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      initContainers:
      - name: db-migrate
        image: reg.capturecore.org/capturecore/hivemind-orchestrator:latest
        imagePullPolicy: Always
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
        command: ["/app/orchestrator", "-migrate"]
        envFrom:
        - secretRef:
            name: orchestrator-env
        env:
        - name: AGENT_NAMESPACE
          value: "hivemind"
        - name: SSL_CERT_FILE
          value: /etc/ssl/capturecore/ca.crt
        volumeMounts:
        - name: ca-cert
          mountPath: /etc/ssl/capturecore
          readOnly: true
      containers:
      - name: orchestrator
        image: reg.capturecore.org/capturecore/hivemind-orchestrator:latest
        imagePullPolicy: Always
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
        command: ["/app/orchestrator", "-serve"]
        ports:
        - containerPort: 8080
          name: http
        envFrom:
        - secretRef:
            name: orchestrator-env
        env:
        - name: SSL_CERT_FILE
          value: /etc/ssl/capturecore/ca.crt
        volumeMounts:
        - name: repo-storage
          mountPath: /app/workspace/repos
        - name: workspace
          mountPath: /app/workspace
        - name: ca-cert
          mountPath: /etc/ssl/capturecore
          readOnly: true
        livenessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /readyz
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
      volumes:
      - name: repo-storage
        persistentVolumeClaim:
          claimName: orchestrator-repos
      - name: workspace
        emptyDir: {}
      - name: ca-cert
        configMap:
          name: capturecore-ca