apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hivemind
  namespace: argocd
  labels:
    app.kubernetes.io/component: ai-platform
  annotations:
    argocd.argoproj.io/sync-wave: "6"
  finalizers:
    - resources-finalizer.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://gitlab.capturecore.org/ai/hivemind-deploy.git
    targetRevision: main
    path: orchestrator/base
    kustomize:
      images:
        - hivemind-orchestrator=reg.capturecore.org/hivemind/hivemind-orchestrator:v0.1.0
        - hivemind-opencode=reg.capturecore.org/hivemind/hivemind-opencode:v0.1.0
  destination:
    server: https://kubernetes.default.svc
    namespace: hivemind
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true